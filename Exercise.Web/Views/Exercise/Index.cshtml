@model IEnumerable<Exercise.Models.ExerciseListItem>

@{
    ViewBag.Title = "Index";
}

<div class="container">
    <div class="row col-md-offset-5">
        
    </div>
</div>


<div class="container">
    <div class="row col-md-offset-4">
        <div>
            <h2 class="col-xs-offset-1">Workout Log</h2>
        </div>
        <div>
            @Html.ActionLink("Create New", "Create", new { }, new { @class = "btn btn-primary btn-large btn-primary " })
            @Html.ActionLink("View Progress", "Progress", new { id = 1 }, new { @class = "btn btn-primary btn-large btn-success" })
        </div>
    </div>
</div>

<br />

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ExcerciseId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Intensity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Duration)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CaloriesBurned)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CreatedUTC)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ExcerciseId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Intensity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Duration)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CaloriesBurned)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreatedUTC)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.ExcerciseId  })*@ 
            @*@Html.ActionLink("Details", "Details", new { id = 1})*@ 
            @*@Html.ActionLink("Delete", "Delete", new { id = item.ExcerciseId })*@
        </td>
    </tr>
}

</table>
